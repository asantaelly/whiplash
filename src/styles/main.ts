import { StyleSheet } from "react-native";

const main = StyleSheet.create({
  shadow: {
    shadowOffset: {
      width: 10,
      height: 10,
    },
    elevation: 10,
    shadowRadius: 12,
    shadowOpacity: 0.9,
    shadowColor: "#000000",
    backgroundColor: "rgba(186, 205, 222, 0.6)",
  },
});

export default main;
